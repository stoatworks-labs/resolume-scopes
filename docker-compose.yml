# Generated by stoatworks-unraid/scripts/gen-docker.mjs from fleet.json.
# Repo: resolume-scopes. Edit fleet.json and regenerate — direct edits are overwritten.
#
# Pull the published image by default. Uncomment build: . to build from this
# checkout instead — useful when testing a change before it reaches GHCR.

services:
  resolume-scopes-demo:
    image: ghcr.io/stoatworks-labs/resolume-scopes-demo:latest
    # build: .
    container_name: resolume-scopes-demo
    restart: unless-stopped
    ports:
      - "8506:80"
