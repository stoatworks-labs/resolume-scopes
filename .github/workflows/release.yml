name: release

# Build the Scopes FFGL plugin for the two platforms Resolume runs on:
#   - macOS   -> universal (arm64 + x86_64) Scopes.bundle
#   - Windows -> x64 Scopes.dll (GLEW static-linked via vcpkg)
# Each ships as a zip; drop the bundle/dll into Resolume's "Extra Effects"
# folder. Trigger manually (Actions > release > Run workflow) or by pushing a
# v* tag.
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - { name: macos-universal, os: macos-14,       artifact: Scopes.bundle }
          - { name: windows-x86_64,  os: windows-latest, artifact: Scopes.dll }
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      # --- macOS: no extra deps, universal build straight from CMake.
      #     Tools are off here: sctest needs a GPU and hosted macOS runners
      #     have none, so it could be built but never usefully run. The
      #     measurement is verified locally instead -- see AGENTS.md. ---
      - name: Configure (macOS)
        if: runner.os == 'macOS'
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release -DSCOPES_BUILD_TOOLS=OFF

      # --- Windows: GLEW comes from vcpkg, static-linked (static-md keeps the
      #     CRT dynamic so the plugin matches the host) so the .dll has no
      #     external glew32.dll runtime dependency. ---
      - name: Configure (Windows)
        if: runner.os == 'Windows'
        run: >
          cmake -B build -A x64
          -DCMAKE_BUILD_TYPE=Release
          -DSCOPES_BUILD_TOOLS=OFF
          -DVCPKG_TARGET_TRIPLET=x64-windows-static-md
          -DCMAKE_TOOLCHAIN_FILE="$env:VCPKG_INSTALLATION_ROOT/scripts/buildsystems/vcpkg.cmake"

      - name: Build
        run: cmake --build build --config Release --parallel

      - name: Derive version
        id: ver
        shell: bash
        run: |
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            echo "version=${GITHUB_REF#refs/tags/v}" >> "$GITHUB_OUTPUT"
          else
            echo "version=0.0.0-dev.${GITHUB_RUN_NUMBER}" >> "$GITHUB_OUTPUT"
          fi

      - name: Collect artifact
        shell: bash
        run: |
          mkdir -p out
          if [ "${{ runner.os }}" = "macOS" ]; then
            cp -R "build/Scopes.bundle" "out/Scopes.bundle"
            # Sanity-check the bundle really is universal and exports plugMain.
            # The build log will call an arm64-only build a success, and the
            # static-constructor registration can be dropped by the linker
            # without a word -- so check the artefact, never the log.
            lipo -archs "out/Scopes.bundle/Contents/MacOS/Scopes"
            # Actions runs `shell: bash` as `bash -eo pipefail`, so `cmd | grep -q X`
            # FAILS when grep FINDS its match: grep exits at once, the writer takes
            # SIGPIPE, and pipefail reports the pipeline as failed -- failing the
            # release on a correct binary. It is output-size dependent, so it bites
            # the biggest artefact first. Captured and matched with `case` instead.
            __archs=$( lipo -archs "out/Scopes.bundle/Contents/MacOS/Scopes" )
            case "$__archs" in
              *x86_64*) ;;
              *) echo "expected x86_64, got: $__archs"; exit 1 ;;
            esac
            __archs=$( lipo -archs "out/Scopes.bundle/Contents/MacOS/Scopes" )
            case "$__archs" in
              *arm64*) ;;
              *) echo "expected arm64, got: $__archs"; exit 1 ;;
            esac
            __symbols=$( nm -gU "out/Scopes.bundle/Contents/MacOS/Scopes" )
            case "$__symbols" in
              *_plugMain*) ;;
              *) echo "expected _plugMain, got: $__symbols"; exit 1 ;;
            esac
          else
            cp "build/Release/Scopes.dll" "out/Scopes.dll"
          fi
          ls -R out

      - name: Archive (zip)
        shell: bash
        run: |
          cd out
          if command -v 7z >/dev/null 2>&1; then
            7z a "../resolume-scopes-${{ matrix.name }}.zip" ./*
          else
            zip -r "../resolume-scopes-${{ matrix.name }}.zip" ./*
          fi

      - name: Disk image (macOS)
        if: runner.os == 'macOS'
        shell: bash
        # A .dmg, but deliberately NOT a .pkg. An FFGL plugin's destination is
        # per-user and edition-specific -- the README installs to
        # ~/Documents/Resolume {Arena,Avenue}/Extra Effects -- so a .pkg would
        # have to guess a path and would silently install to the wrong one for
        # half of users. Mounting an image and dragging the bundle where the
        # README says is the honest shape here.
        run: |
          source scripts/release-lib.sh
          rl_init "Scopes" resolume-scopes "${{ steps.ver.outputs.version }}" \
            com.stoatworks.ffgl.scopes "$GITHUB_WORKSPACE"
          cp README.md out/ 2>/dev/null || true
          rl_adhoc_sign "out/Scopes.bundle"
          rl_dmg "${{ matrix.name }}" out

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: resolume-scopes-${{ matrix.name }}
          path: |
            *.zip
            *.dmg
          if-no-files-found: error

  windows-installer:
    name: Windows installer
    needs: build
    runs-on: ubuntu-latest
    # makensis cross-compiles, so the installer is wrapped here from the zip the
    # Windows job uploaded. Its directory page defaults to the common FFGL
    # folder but stays editable, because Resolume reads plugins from a path the
    # user chooses per edition.
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Install NSIS
        run: sudo apt-get update && sudo apt-get install -y nsis

      - name: Build installer
        shell: bash
        run: |
          source scripts/release-lib.sh
          version="${GITHUB_REF_NAME#v}"
          case "$version" in [0-9]*) ;; *) version="0.0.0" ;; esac
          rl_init "Scopes" resolume-scopes "$version" com.stoatworks.ffgl.scopes "$PWD/installers"
          zip="$(find artifacts -name "resolume-scopes-windows-x86_64.zip" | head -1)"
          if [ -z "$zip" ]; then echo "no Windows artefact, nothing to do"; exit 0; fi
          stage="$RUNNER_TEMP/stage-win"
          rm -rf "$stage"; mkdir -p "$stage"
          unzip -q "$zip" -d "$stage"
          cp README.md "$stage/" 2>/dev/null || true
          # --cli: a plugin has no executable to make a shortcut to.
          rl_nsis windows-x86_64 "$stage" --cli
          ls -l installers

      - uses: actions/upload-artifact@v7
        with:
          name: resolume-scopes-windows-installer
          path: installers/*
          if-no-files-found: warn

  release:
    name: Publish release
    needs: [build, windows-installer]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
      - uses: softprops/action-gh-release@v3
        with:
          files: artifacts/**/*
          generate_release_notes: true
